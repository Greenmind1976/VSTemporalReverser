# TODO

- Organize the mod configuration page so the wood, critter spawn, and debug settings are easier to read and group cleanly in ConfigLib.

## Nice to Have / Future Update

- Revisit the `Temporal Reconstruction Device` open-state presentation. Options to consider:
  - sliding top-panel texture swap when opened
  - open-state shape variant with the top gear hidden
  - possible animated hatch/lid later if the final machine silhouette warrants it
- Investigate a `broken tool` flow where tools can hit `0` durability, remain as broken items, and be restored by the `Temporal Reconstruction Device` instead of disappearing outright.
- Revisit `restored-decoration` for decorative book clutter. Goal: restored cartography books, large books, book rows, and book piles should become easy-to-place decorative items instead of falling back to loose books.
- Investigate making specialty bookshelves drop something more specific than generic books.
- Change the book stand from a shelf variant into its own restored item.
